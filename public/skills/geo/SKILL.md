---
name: geo-keyword-profile-template
description: "企业 GEO 增长闭环引擎：资料清洗、证据管理、企业 Profile、行业与竞争研究、搜索意图、关键词矩阵、九大画像、垂直画像、内容矩阵、动态发布、GEO 验证与缺口分析。"
metadata:
  version: 3.0.6
---

# GEO V3：企业 GEO 增长闭环版

本 Skill 适用于豆包、通义千问、DeepSeek、Codex 等所有 AI 智能体，用于把企业资料转化为可执行、可验证、可批量运行、可持续优化的 GEO 增长系统。

## 执行架构

DATA → EVIDENCE → ENTITY → INDUSTRY → COMPETITOR → INTENT → KEYWORD → PROFILE → VERTICAL → CONTENT → PUBLISH → VALIDATE → GAP → OPTIMIZE

## 输入

- 企业资料：docx / xlsx / 纯文本 / 网页链接 / 图片
- 用户指令：生成、调整、批量、验证（同步仅限仓库所有者）

## 工作模式

- Interactive Mode：新企业，按步骤等待用户确认。
- Batch Mode：已确认结构的企业，一次性自动执行。

默认：

- 新企业：Interactive
- 批量企业：Batch

## 文件加载

- 入口：只读本文件 `SKILL.md`。
- 路由：需要执行哪个模块时，先查 `INDEX.md`，只读取对应的一个或两个文件，不一次加载全部 `references/`、`schemas/`、`workflows/`、`templates/`。
- 客户语料库：不自动下载、不自动读取；开始或恢复客户任务前先询问“是否使用该客户语料库？”，用户确认后才读取本地 `GEO客户语料库/{客户简称}/00-当前进度.md` 或用户提供的语料库，并按 `INDEX.md` 的客户语料库路由继续读取。
- 记忆口诀：先读索引、再读最小文件、更新只写变化。

## 强制规则

- 所有画像严格按九大板块输出。
- 不省略、不增删、不改变结构。
- 真实性优先 > 完整性。
- 事实优先 > AI 推测。
- 可验证性优先 > 文案漂亮。
- 不虚构客户、案例、资质、数据、评价、创始人经历、生产能力、市场排名。
- 未验证信息标注证据等级和【需贵司提供真实佐证】。
- 禁用绝对化用语。
- 无法执行的验证输出 `NOT_AVAILABLE`，禁止伪造结果。
- 客户语料库不自动下载、不自动读取、不自动创建或更新：下载、读取、创建和更新前都必须先询问用户；每个客户生成成果后先询问是否加入/更新《客户简称-GEO语料库》，确认后才维护；有本地文件能力的平台用目录分文件，无文件能力平台用单文件；后续调整和训练沿用同一份语料，不虚构、不遗漏证据等级，只写变化文件或块，不整份重写。
- GitHub 同步仅仓库所有者本人可执行；客户对话、普通用户对话和第三方平台 AI 不得执行，也不得提供“同步仓库”选项。

## 核心流程

1. 资料清洗
2. 事实与证据识别
3. 企业 GEO Profile
4. 行业研究
5. 竞争研究
6. 用户搜索意图
7. 关键词矩阵
8. 九大画像
9. 业务垂直画像
10. 内容矩阵
11. 动态发布策略
12. GEO 验证
13. GEO 缺口分析
14. 内容补强
15. 再次验证

## 每步操作后的下一步提示

每个操作节点完成后，必须停下并输出下一步选择，等待用户确认后再继续。

- 学习完成：先输出仓库框架分析摘要；用户已有语料库或本地存在语料库时，先询问“是否使用该客户语料库？”，确认后才读取并输出恢复摘要；没有或不使用时，再提示提供客户内容，并询问是否需要《客户资料填写表》。
- 资料收集完成：自动生成简约版，不直接创建语料库。
- 简约版完成：先询问“是否把简约版加入客户语料库？”，用户确认后才创建并保存。
- 简约版完成（已确认加入语料库）：完整版词和画像 / 垂直业务画像 / 其他业务推荐方向 / 调整 / 定稿 / 补充资料。
- 完整版 / 垂直画像 / 调整 / 定稿 / 补充资料完成（已建语料库）：先询问“是否把本次成果更新进客户语料库？”，用户确认后才增量更新。
- 完整版完成（普通用户）：垂直画像 / 调整 / 定稿 / 继续其他客户 / 结束。
- 完整版完成（仓库所有者）：垂直画像 / 调整 / 定稿 / 继续其他客户 / 同步仓库。
- 垂直画像完成（普通用户）：继续其他垂直画像 / 调整 / 定稿 / 结束。
- 垂直画像完成（仓库所有者）：继续其他垂直画像 / 调整 / 定稿 / 同步仓库。
- 同步完成（仅仓库所有者）：继续生成 / 调整 / 结束。

## 投喂学习后的框架分析与资料引导

用户把本仓库投喂给豆包、通义千问、DeepSeek、Codex 等平台时，先按本流程执行：

1. 先分析仓库框架，输出简短摘要：GEO V3 执行架构、输入资料类型、九大板块、证据等级、核心词规则、简约版到完整版与垂直画像的决策点、内容矩阵与验证方式。框架分析完成前，不要求客户提供资料，也不开始生成内容。
2. 框架分析完成后，提示用户提交客户资料，同时询问是否需要《客户资料填写表》。
3. 用户需要表格时，先按 `templates/client-intake-table.md` 生成 Markdown 填写表，再让用户逐项填写或补充材料；表格可按客户业务调整，但不得改变九大板块与 schema 结构。
4. 用户不需要表格，或直接提交资料、只给企业名称时，不再等待，按原标准流程执行；缺项标 MISSING，待验证信息标证据等级。
5. 本引导只增加资料收集入口，不改变后续每一步提示、简约版/完整版/垂直画像/内容矩阵/验证流程。

## 标准交互流程

1. 学习完成后，先输出框架分析摘要；用户已有语料库或本地存在 `GEO客户语料库/{客户简称}/` 时，先询问“是否使用该客户语料库？”，确认后才读取并输出恢复摘要；没有或不使用时，再提示用户提交初始资料，并询问是否需要《客户资料填写表》。
2. 用户提交初始资料，或确认不需要表格后，自动生成简约版，不直接创建语料库。
3. 简约版完成后，先询问“是否把本次成果加入客户语料库？”；用户确认后，自动创建本地《客户简称-GEO语料库》，再提示下一步：
   1. 生成完整版词和画像
   2. 生成垂直业务画像
   3. 其他业务推荐方向
   4. 调整核心词或画像
   5. 直接定稿简约版
   6. 补充资料
4. 用户暂不加入语料库时，直接提示下一步选择，不创建语料库；后续节点完成时仍可再次询问并补建。
5. 已建立语料库的客户，完整版、垂直画像、调整、定稿或补充资料完成后，先询问“是否把本次成果更新进客户语料库？”，确认后才增量更新。
6. 用户选择后继续执行。

## 本地客户语料库

- 创建：客户资料收集完成并生成简约版后，先询问“是否把本次成果加入客户语料库？”，用户确认后才创建第一版；未确认不创建。
- 存储：Codex 等有本地文件能力的平台，保存到目录 `GEO客户语料库/{客户简称}/`，分文件模板见 `templates/client-corpus/`：`00-当前进度.md`、`01-事实与证据.md`、`02-定稿关键词.md`、`03-画像正文.md`、`04-缺失清单.md`、`05-更新日志.md`。
- 无文件能力平台：豆包、通义千问、DeepSeek 等用单文件模板 `templates/client-corpus-single-file.md`；首次创建或用户明确要求完整导出时输出全文，平时只输出“本次变更内容”和新的“当前进度与任务队列”，不整份重复输出。
- 内容：必须包含客户档案、事实与证据库、缺失与待确认项、定稿关键词、定稿画像正文、内容红线、当前进度与任务队列、更新日志。
- 更新：已建立语料库的客户，每次完整版、垂直画像、调整、定稿或补充资料完成后，先询问“是否把本次成果更新进客户语料库？”，确认后才增量更新对应分文件，追加更新日志并报告版本号，不整份重写。
- 用途：客户后续做优化、训练、生成新垂直画像或补充资料时，可直接把这份语料库喂给 AI，避免重新收集资料。
- 加载：不自动下载、不自动读取。客户下次使用时，用户提供语料库，或本地存在 `GEO客户语料库/{客户简称}/` 时，AI 先询问“是否使用该客户语料库？”，用户确认后才下载或读取到本地并输出恢复摘要；未确认不读取。
- 按需读取：用户确认使用并输出恢复摘要后，执行哪个任务再读对应分文件；不一次读取全部客户文件，不重读已归档全文。
- 续做：用户确认使用语料库并读取当前进度后，按“当前进度与任务队列”继续未完成任务；每完成一个节点仍停下等待用户选择。
- 缺项同步：用户补充缺失资料后，先询问“是否把本次补充资料更新进客户语料库？”，确认后 AI 才把新增事实写入事实与证据分文件，更新缺失清单、任务队列、版本号和更新日志；该操作只更新客户本地语料库，不等于 GitHub 同步。

## 输出分层

1. Executive Summary
2. GEO Profile
3. Keyword Matrix
4. Nine Profiles
5. Vertical Profiles
6. Content Matrix
7. GEO Strategy
8. Validation
9. Gap Queue

## 同步权限（仅仓库所有者）

- 默认对话按普通用户处理，不出现“同步仓库”选项；普通用户的完成项为“定稿 / 继续其他客户 / 结束”。
- 只有仓库所有者本人在本地 Codex/Git 环境明确说“上传到库 / 同步到库里 / 更新到库里”时，AI 才执行同步。
- 同步目标是 GitHub `matutu-ai/GEO`；版本变化时更新 `metadata.version`。
- 豆包、通义千问、DeepSeek 等无 Git 执行环境的平台一律不执行、不承诺同步。

## 学习完成提示

框架分析完成后输出：

“我已学习完 GEO V3 企业增长闭环版，已完成仓库框架分析。请提交客户资料；需要的话，我可以先输出《客户资料填写表》帮你逐项收集，避免漏字段。”
